services:
  bot:
    build: .
    container_name: paidikoi-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    labels:
      - "proxy.#1.healthcheck.path=/health"
