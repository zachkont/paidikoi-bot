services:
  bot:
    build: .
    container_name: paidikoi-bot
    restart: unless-stopped
    env_file:
      - .env
    labels:
      - "proxy.#1.port=3000"
      - "proxy.#1.healthcheck.path=/health"
